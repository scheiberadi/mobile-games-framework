apply plugin: 'com.android.library'
apply from: '../shared/keepUnitySymbols.gradle'
apply from: '../shared/common.gradle'
**APPLY_PLUGINS**

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
**DEPS**}

// Sudoku has no ads/IAP of its own, but the GoogleMobileAds SDK stays linked project-wide
// so 2048's ads keep working - its play-services-ads AAR unconditionally declares the
// AD_ID permission, which Play Console flags as an "Incomplete advertising ID
// declaration" since Sudoku truthfully declares it doesn't use advertising ID. Excluding
// the dependency here (Sudoku-only - this file is copied in and deleted around
// BuildSudoku/BuildSudokuRelease only, see AndroidApkBuilder.cs) keeps the AAR, and the
// permission it brings, out of Sudoku's build entirely instead of trying to strip the
// permission back out after the fact via a manifest merge override - that approach was
// tried previously and is confirmed broken: a custom AndroidManifest.xml in this same
// Assets/Plugins/Android folder makes Gradle regenerate the unityLibrary module without
// its launcher activity, which is a module-generation issue, not a mergeable one. This
// exclusion instead works at plain Gradle dependency resolution, verified via a debug
// build + aapt2 dump badging (AD_ID gone, launcher activity and icon intact) and an
// on-device install before being wired into the release build.
configurations.all {
    exclude group: 'com.google.android.gms', module: 'play-services-ads'
    exclude group: 'com.google.android.gms', module: 'play-services-ads-lite'
    exclude group: 'com.google.android.gms', module: 'play-services-ads-identifier'
    // play-services-ads pinned kotlin-stdlib's version across the whole dependency
    // graph; removing it above lets Gradle pull in an older kotlin-stdlib-jdk7/jdk8
    // split artifact that now duplicates classes already in the main kotlin-stdlib
    // artifact (merged there since Kotlin 1.8) - drop the old split artifacts instead
    // of the base one so this resolves the same way it used to.
    exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk7'
    exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk8'
}

android {
    namespace "com.unity3d.player"
    ndkPath "**NDKPATH**"
    ndkVersion "**NDKVERSION**"
    compileSdk **APIVERSION**
    buildToolsVersion "**BUILDTOOLS**"

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    defaultConfig {
        minSdk **MINSDK**
        targetSdk **TARGETSDK**
        ndk {
            abiFilters **ABIFILTERS**
            debugSymbolLevel **DEBUGSYMBOLLEVEL**
        }
        consumerProguardFiles 'proguard-unity.txt'**USER_PROGUARD**
**DEFAULT_CONFIG_SETUP**
    }

    lint {
        abortOnError false
    }

    androidResources {
        noCompress = **BUILTIN_NOCOMPRESS** + unityStreamingAssets.tokenize(', ')
        ignoreAssetsPattern = "!.svn:!.git:!.ds_store:!*.scc:!CVS:!thumbs.db:!picasa.ini:!*~"
    }**PACKAGING**
}
**IL_CPP_BUILD_SETUP**
**SOURCE_BUILD_SETUP**
**EXTERNAL_SOURCES**
